{
  "name": "@todoer/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "todoer": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@todoer/specs": "workspace:^",
    "uuidv7": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.6.3",
    "vitest": "^5.0.2"
  }
}
