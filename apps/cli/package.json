{
  "name": "@todoer/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "todoer": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@todoer/specs": "workspace:^",
    "uuidv7": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^5.0.2"
  }
}
