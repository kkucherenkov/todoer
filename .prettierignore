# Regenerated by `pnpm spec:codegen`; its shape is the generator's, not ours.
packages/specs/src/generated/
pnpm-lock.yaml
# Prettier rewrites emphasis, list continuations and code inside fences —
# churn in ADRs and plans that are records, not code.
*.md
